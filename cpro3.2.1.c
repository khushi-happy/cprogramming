#include <stdio.h>

int main() {
    int a, b, c;
    float d;

    printf("Enter coefficient of a: ");
    scanf("%d", &a);

    printf("Enter coefficient of b: ");
    scanf("%d", &b);

    printf("Enter coefficient of c: ");
    scanf("%d", &c);

    d = b * b - 4 * a * c;

